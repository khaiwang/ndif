--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.7,<2.11
# Pypi nnsight installed first; the Dockerfile then overrides with the
# local hf-serve worktree (which ships VanillaBatchServer). Once
# VanillaBatchServer reaches a release, this line + the bind-mount can
# move to a pinned version.
nnsight
diffusers
ray[serve]==2.50.0
grpcio<1.80
python-slugify
# Telemetry
prometheus_client
loki-logger-handler
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-grpc
# Database
boto3
influxdb-client
# Google Cal Scheduling
google-api-python-client
google

# Security
RestrictedPython>=7.0
zstandard
pandas
